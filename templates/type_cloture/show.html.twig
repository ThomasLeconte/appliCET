{% extends '@NormandieView/base.html.twig' %}

{% block title %}TypeCloture{% endblock %}

{% block body %}

	{{ bloc("Détail d'un type de clôture ", {expanded: true, expandable: false, "infobulle": info_bulle("") }) }}

    <div class="form" style="width:50%;--label-width:15em;">

                	                	        	
				<div class="form_row">
					<span class="form_label"><label>Libelle</label></span>
					<div class="form_row_widget">{{ type_cloture.libelle }}</div>
                </div>
                
        {% if is_granted('ROLE_DSI') %}
		<div class="form_buttons">
           	{{ button("Retour à la liste", {icone:'annule', url: path('type_cloture_index')}) }}
        </div>
 		{% endif %}		                

    </div>
    
    {{ fin_bloc() }}
    
{% endblock %}